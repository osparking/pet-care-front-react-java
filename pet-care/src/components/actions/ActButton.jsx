import React from "react";

const ActButton = ({
  title,
  variant,
  onClick,
  disabled,
  isProcessing,
  className = "",
}) => {
  return <div>ActButton</div>;
};

export default ActButton;
